# Smart Permission Flow Example

This Flutter app demonstrates the `smart_permission_flow` package with camera, location, notification, and multi-permission request flows.

## Run

```sh
flutter pub get
flutter run
```

The app uses the local package through a path dependency:

```yaml
smart_permission_flow:
  path: ..
```
